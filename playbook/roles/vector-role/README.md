# Vector Role

Ansible роль для установки и настройки Vector.

## Переменные

| Переменная | Значение по умолчанию | Описание |
|------------|----------------------|----------|
| `vector_version` | "0.39.0" | Версия Vector для установки |

## Пример использования

```yaml
- hosts: vector
  roles:
    - role: vector_role
      vector_version: "0.40.0"


## 🚨 Шаг 3: Создаем роль Lighthouse

```bash
# Создаем каркас роли
ansible-galaxy role init roles/lighthouse-role

# Создаем директорию для шаблонов
mkdir -p roles/lighthouse-role/templates